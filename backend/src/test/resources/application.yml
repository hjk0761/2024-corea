spring:
  application:
    name: backend
  jpa:
    defer-datasource-initialization: true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create-drop
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:database

logging:
  level:
    root: INFO
    corea: DEBUG

springdoc:
  swagger-ui:
    groups-order: DESC
    tags-sorter: alpha
    operations-sorter: method
    disable-swagger-default-url: true
    display-request-duration: true
    defaultModelsExpandDepth: 2
    defaultModelExpandDepth: 2
  api-docs:
    path: /api-docs
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json
  writer-with-default-pretty-printer: true
  model-and-view-allowed: true
  paths-to-match:
    - /**

management:
  endpoint:
    web:
      exposure:
        include: health

security:
  jwt:
    token:
      secret-key: AasldkfjlawekjlJlkdjaiwejrvo43ijlakfdvg3l3kasldkfgl45jlij4lijlaid
      expire-length:
        access: 1800
        refresh: 604800

oauth:
  github:
    client_id: Ov23liFoUtO89lG70w9I
    client_secret: 8cfde5525b15e0e01e5d533599f07530cc2bf264

github:
  baseUrl:
    oauth: https://github.com/login/oauth/access_token
    user: https://api.github.com/user
